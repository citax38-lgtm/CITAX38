{
  "name": "gestione-turni",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "rm -rf dist && mkdir dist && cp index.html dist/index.html && cp index.css dist/index.css && esbuild index.tsx --bundle --outfile=dist/bundle.js",
    "sync:android": "npm run build && npx cap sync android",
    "open:android": "npx cap open android"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@capacitor/android": "^6.0.0",
    "@capacitor/cli": "^6.0.0",
    "@capacitor/core": "^6.0.0",
    "esbuild": "^0.20.2"
  }
}
